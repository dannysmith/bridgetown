---
name: Documentation Note
description: This shows up **in the** docs _folder_.
variables:
  title?: ["string", "Title for the note"]
  type?: ["string", "Specify `warning` for a red note"]
  extra_margin?: boolean
  content: markdown
---
{%- if extra_margin -%}
{%- assign extra_margin_class = "my-10" -%}
{%- endif -%}

<div class="note {{ type }} {{ extra_margin_class }}">
  {%- if title -%}
    <h5>{{ title }}</h5>
  {%- endif -%}

  {{- content -}}
</div>
